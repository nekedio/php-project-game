name: linter

on: [push]


jobs:
  test_linter:

    runs-on: ubuntu-18.04

    steps:
    - name: Setup PHP
      with:
        php_version: '7.2'
    - name: Install package
      run: make install
    - name: Start linter
      run: make lint

